# Kernel

Notes about what a kernel is and what responsibilities it has inside an operating system.

## Questions to Study

- What is a kernel?
- What does the kernel control?
- What is kernel mode?
- What is user mode?
- Why does the kernel need direct access to hardware?
